# 自己最適化のための開発効率向上テックドック

## 1.0 プロジェクト概要とAIエージェントの役割

### 課題と解決策
本プロジェクト「TableDefUtil」は、システム開発における「テーブル定義（Excel/CSV/TSV）からソースコードを手作業で作成する」という反復的でエラーが発生しやすい作業を自動化するためのツールです。
解決策として、ブラウザ上で完結する（サーバーサイドを持たない）ローカルファーストなSingle Page Application (SPA)を提供し、セキュリティリスクを最小限に抑えつつ、多様な言語（Java, TypeScript, SQL）への変換を実現しています。

### 全体アーキテクチャ
- **Client-Side Only**: サーバー処理は一切行わず、全ての変換ロジックはブラウザのJavaScriptで実行されます。
- **Modular Monolith (JS)**: バニラJavaScriptを使用し、機能ごとにファイルを分割して[index.html](file:///Users/yamagh/Documents/GitHub/TableDefUtil/index.html)で読み込むシンプルな構成です。ビルドツール（Webpack/Viteなど）は**使用していません**。
- **State Management**: 単一のグローバルオブジェクト (`AppState`) を使用してアプリケーションの状態を管理します。

[Image: System Architecture Diagram]

### AIエージェントの機能範囲 And 役割
- **新規コンバータの追加**: 新しい言語やフレームワーク（例: Go, Python, GraphQL）への変換ロジックの実装。
- **UI/UXの改善**: 既存のCSSフレームワーク (Pico.css) に基づいたUIコンポーネントの追加・修正。
- **リファクタリング**: [main.js](file:///Users/yamagh/Documents/GitHub/TableDefUtil/js/main.js)等の肥大化を防ぐための、適切なロジック分割とモジュール化の推進。

---

## 2.0 🛠️ 必須ツールと環境構築ガイド

### 開発環境
特別なビルドツールやパッケージマネージャーは**必須ではありません**が、以下の環境を推奨します。

1.  **エディタ**: VS Code
2.  **ローカルサーバー**: 静的ファイルを配信できる任意のサーバー
    - 推奨: `Live Server` (VS Code Extension)
    - 代替: `python3 -m http.server 8000`
3.  **ブラウザ**: 最新の Chrome / Edge / Firefox (ES6+ 対応)

### 必須ライブラリ (Vendor)
本プロジェクトは外部ライブラリを `vendor/` ディレクトリに配置し、Git管理しています。**npm installは使用しません**。
- **Pico.css**: CSSフレームワーク ([vendor/css/pico.min.css](file:///Users/yamagh/Documents/GitHub/TableDefUtil/vendor/css/pico.min.css))
- **Papa Parse**: CSV/TSVパーサー ([vendor/js/papaparse.min.js](file:///Users/yamagh/Documents/GitHub/TableDefUtil/vendor/js/papaparse.min.js))
- **JSZip**: ZIPファイル生成 ([vendor/js/jszip.min.js](file:///Users/yamagh/Documents/GitHub/TableDefUtil/vendor/js/jszip.min.js))
- **SQL Formatter**: SQL整形 ([vendor/js/sql-formatter.min.js](file:///Users/yamagh/Documents/GitHub/TableDefUtil/vendor/js/sql-formatter.min.js))

### アクセス情報
- **ローカル**: `http://localhost:5500/index.html` (Live Server使用時)
- **外部API**: 現時点で使用していません。OpenAI API等を組み込む場合は、ユーザーにAPIキーを入力させるUIを作成し、`localStorage` またはメモリ内でのみ保持してください（サーバーに送信しないこと）。

---

## 3.0 📜 AIエージェント向けコーディング標準とガードレール

### 命名規則
- **ファイル名**: `snake_case` (例: [java_model.js](file:///Users/yamagh/Documents/GitHub/TableDefUtil/js/converters/java_model.js), `sql_ui.js`)
- **クラス/オブジェクト名**: [PascalCase](file:///Users/yamagh/Documents/GitHub/TableDefUtil/js/utils/common.js#16-22) (例: `AppState`, `SqlUi`, `Zipper`)
- **関数名**: `camelCase` (例: [generateJavaModel](file:///Users/yamagh/Documents/GitHub/TableDefUtil/js/converters/java_model.js#1-182), `updateSqlOutput`)
- **変数名**: `camelCase` (例: `parsedTables`, `rlsOptions`)

### コード構造パターン
ES Modules (`import`/`export`) は**使用禁止**です。[index.html](file:///Users/yamagh/Documents/GitHub/TableDefUtil/index.html) の `<script>` タグの読み込み順序に依存したグローバルスコープ設計に従ってください。

#### 1. モジュール定義
```javascript
// js/core/example_module.js
const ExampleModule = {
  data: [],
  init() {
    // 初期化ロジック
  },
  doSomething() {
    // 処理
  }
};
```

#### 2. グローバルステートの利用
状態は必ず `AppState` (defined in [js/core/state.js](file:///Users/yamagh/Documents/GitHub/TableDefUtil/js/core/state.js)) を介してアクセスしてください。
```javascript
// Bad
let myTables = []; 

// Good
AppState.parsedTables = newTables;
```

### ⚠️ エージェントへの絶対禁止事項 (Guardrails)
1.  **npm/yarnの使用禁止**: ビルドステップ（Webpack, Babel等）を導入しないでください。ブラウザが直接実行できるコードのみを記述すること。
2.  **サーバーサイドコードの混入**: Node.js専用のAPI（`fs`, `path`など）を使用しないでください。
3.  **グローバル汚染の最小化**: 可能な限り、関連する関数はオブジェクト（名前空間）にまとめてください。
4.  **既存のHTML構造の破壊**: `id` 属性に依存しているJavaScriptが多数存在します。HTMLの修正時はの影響範囲を必ず検索 (`grep`) して確認してください。

---

## 4.0 🔗 コアコンポーネントと設計パターン

### 主要コンポーネント
| コンポーネント | ファイルパス | 説明 |
| :--- | :--- | :--- |
| **`AppState`** | [js/core/state.js](file:///Users/yamagh/Documents/GitHub/TableDefUtil/js/core/state.js) | アプリケーション全体のSingle Source of Truth。パース済みテーブルデータやSQLビルダーの状態を保持。 |
| **`Parser`** | [js/core/parser.js](file:///Users/yamagh/Documents/GitHub/TableDefUtil/js/core/parser.js) | `Papa Parse` の結果をアプリ内部の標準フォーマット（中間データ）に変換する。 |
| **`Zipper`** | [js/core/zipper.js](file:///Users/yamagh/Documents/GitHub/TableDefUtil/js/core/zipper.js) | 各コンバータを呼び出し、結果を集約してZIPファイルを生成するFacade。 |

### 中間データ構造 (`Table` オブジェクト)
[Image: Data Flow Diagram]
全コンバータはこのデータ構造を入力として受け取ります。

```typescript
type Table = {
  tableName: string;      // 物理名 (例: users)
  tableNameJP: string;    // 論理名 (例: ユーザー)
  descripion: string;     // 説明
  columns: Column[];      // カラムリスト
  indexes: { [key: string]: IndexItem[] }; // インデックス情報
};

type Column = {
  colName: string;        // 物理名 (例: user_id)
  colNameJP: string;      // 論理名 (例: ユーザーID)
  type: string;           // データ型 (例: bigserial)
  length: string;         // 長さ (例: 255)
  constraint: string;     // 制約 (例: NN, U, PK)
  default: string;        // デフォルト値
};
```

### コンバータ実装パターン (Strategy Pattern)
新しいフォーマットを追加する場合は、`js/converters/` にファイルを作成し、以下のシグネチャを持つ関数を定義してください。

```javascript
/**
 * @param {Array} tables - 中間データの配列
 * @param {Object} options - オプション (RLS設定など)
 * @returns {Array<{path: string, content: string}>} 生成ファイルのリスト
 */
function generateNewFormat(tables, options) {
  const files = [];
  tables.forEach(table => {
    files.push({
      path: `dir/${table.tableName}.ext`,
      content: `code content...`
    });
  });
  return files;
}
```
※ `Zipper.js` でこの関数を呼び出すように `switch` 文に追加する必要があります。

---

## 5.0 ✅ テストと検証の要件

### コード品質目標
- **カバレッジ率**: 現状は0%ですが、主要なロジック（パーサー、コンバータ）については、将来的に **Unit Test Coverage $\ge 80\%$** を目指します。
- **テストフレームワーク**: ビルド不要なブラウザ実行可能なテスト（例: Mocha + Chai のCDN版）または、Node.jsで実行可能なシンプルなテストスクリプト(`node tests/test_parser.js`)を推奨します。

### テストケース命名規則
```javascript
// Format: [Module] should [Expected Behavior] when [Condition]
// Example: "JavaModelConverter should generate @NotNull annotation when constraint includes NN"
```

### 検証手順 (Manual Verification)
テストコードがない場合、以下の手順で検証を行ってください。
1.  `samples/input.tsv` を「ファイル入力」タブからロードする。
2.  全てのチェックボックスをONにし、「変換実行」をクリックする。
3.  各タブ（DDL, TypeScript, Javaなど）にコードが表示されるか確認する。
4.  「すべてダウンロード」を実行し、ZIPファイルが解凍できるか、中身が空でないか確認する。

---

## 6.0 🔄 CI/CDとデプロイメント戦略

### デプロイメントフロー
現状は手動デプロイまたはGitHub Pagesの自動デプロイを想定しています。

```mermaid
graph LR
    A[Developers] -->|Push/Merge| B(GitHub Repository)
    B -->|Action (Optional)| C{Build/Test}
    C -->|Pass| D[GitHub Pages / Web Server]
    C -->|Fail| E[Notify Developer]
```

### エージェントによるデプロイメント破壊防止策
1.  **`index.html` の script src 確認**: `js/` フォルダ内のファイルを削除・リネームした際、必ず `index.html` の `<script>` タグも同時に更新してください。読み込み順序が間違っているとアプリが起動しません。
2.  **`vendor/` の保持**: サードパーティライブラリはCDNではなくローカルの `vendor/` から読み込む構成を維持してください（オフライン動作保証のため）。

---

## 7.0 💡 トラブルシューティングとFAQ (AI向け)

### よくあるエラーと診断
| エラーメッセージ / 現象 | 可能性の高い原因 | 解決策 |
| :--- | :--- | :--- |
| **`ReferenceError: X is not defined`** | スクリプトの読み込み順序が間違っている。 | `index.html` で、依存されるファイル（例: `utils`, `core`）を先に読み込むように記述順を修正する。 |
| **CSVパース失敗** | 入力データのタブ/カンマ区切りが不正。 | `Papa Parse` の設定を確認する。TSVの場合は `delimiter: '\t'` が指定されているか確認。 |
| **ZIPダウンロードが始まらない** | `JSZip` がロードされていない、または生成処理でエラー。 | ブラウザのDevToolsコンソールを確認し、例外発生箇所を特定する。 |

### ログと解析
サーバーログは存在しません。ブラウザの **Developer Tools (F12) > Console** が唯一のログソースです。
エラー発生時は `console.error` の内容とスタックトレースを解析してください。

### 過去の教訓 (Self-Correction)
- **教訓1**: ES Modules (`import`/`export`) を使ってしまい、ブラウザで動かなくなったことがある。 -> **対策**: 必ず `<script src="...">` 形式であることを意識する。
- **教訓2**: `AppState` を経由せずDOMから直接値を取ろうとして、UI変更時にロジックが壊れた。 -> **対策**: UI (`sql_ui.js`) とロジック (`sql_logic.js`) の分離を徹底する。ロジックは常にデータ (`parsedTables`) を操作し、UIはデータを描画するだけにする。
